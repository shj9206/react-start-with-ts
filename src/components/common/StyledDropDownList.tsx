import styled from "styled-components";
import {
  DropDownList,
  DropDownListProps,
} from "@progress/kendo-react-dropdowns";

const StyledDropDownList = styled(DropDownList)<DropDownListProps>`
  // 추가 스타일
`;

export default StyledDropDownList;
