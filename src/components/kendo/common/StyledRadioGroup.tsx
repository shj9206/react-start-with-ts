import styled from "styled-components";
import { RadioButtonProps, RadioGroup } from "@progress/kendo-react-inputs";

const StyledRadioButton = styled(RadioGroup)<RadioButtonProps>`
  // 추가 스타일
`;

export default StyledRadioButton;
